doctype html
html(lang='en')
  head
    h1= title 
    title p Welcome to #{title}      
    link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/semantic-ui@2.3.3/dist/semantic.min.css')
    style.
      table.ui.celled img { display: inline-block; }
      footer { margin: 35px 0 15px 0; text-align: center }
  body
    main#main
    form(action='/config', method='POST')
      h1.ui.center.aligned.header Config
      .ui.container
        table.ui.celled.table.center.aligned
          thead
            tr
              th api key
              th domain
              th spName 
          tbody
              tr 
                td 
                  input(name='apiKey', value=apiKey, type='text'  )
                td 
                  input(name='domain', value=domain, type='text'  )
                td
                  input(name='spName', value=spName, type='text' , aria-rowindex=2)
                td
                  input(value='Submit', type='submit' )
          tfoot
            tr
              th(colspan='3')
  footer   
        tr
          a(href= `./${spName}/${domain}/${apiKey}/metadata.xml`   )
        tr
          a(href= `./${spName}/${domain}/${apiKey}/config`)
        tr
          a(href= `./${spName}/${domain}/${apiKey}/metadata.xml`)
        tr
          a(href= `./${spName}/${domain}/${apiKey}/login`)
        tr
          a(href= `./${spName}/${domain}/${apiKey}/logout`)
          //    P()
          //      link(href= `./${spName}/${domain}/${apiKey}/logout`)
          //    P()
          //      link(href= `./${spName}/${domain}/${apiKey}/login`)
      
        //block content
        //    h1= title
        //    p Welcome to #{title}
        //
        //    form(action='/config', method='POST')
        //        P()
        //          | api key: 
        //          input(name='apiKey', value=apiKey, type='text'  )
        //        P() 
        //          | domain: 
        //          input(name='domain', value=domain, type='text', aria-rowindex=1)
        //        P()
        //          | sp name: 
        //          input(name='spName', value=spName, type='text' , aria-rowindex=2)
        //        P() 
        //          input(value='Submit', type='submit' )
        //          
        //block footer
        //
        //  P()
        //      link(href= `./${spName}/${domain}/${apiKey}/metadata.xml`)
        //    P()
        //      link(href= `./${spName}/${domain}/${apiKey}/config`)
        //    P()
        //      link(href= `./${spName}/${domain}/${apiKey}/logout`)
        //    P()
        //      link(href= `./${spName}/${domain}/${apiKey}/login`)